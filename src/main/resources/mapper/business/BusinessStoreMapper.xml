<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gemini.business.business.mapper.BusinessStoreMapper">

    <resultMap id="resultMap" type="com.gemini.business.business.po.BusinessStorePo">
                <id column="id" property="id" javaType="Long"/>
                <result column="name" property="name" javaType="String"/>
                <result column="business_id" property="businessId" javaType="Long"/>
                <result column="business_name" property="businessName" javaType="String"/>
    </resultMap>

    <sql id="fields">
                            s.name,
                    s.business_id,
                    s.business_name
    </sql>

    <sql id="table">
        d_l_risk_dict s
    </sql>

    <sql id="where">
        <where>
                    <if test="name != null and name != ''">
                        and s.name = #{name}
                    </if>
                    <if test="businessId != null and businessId != ''">
                        and s.business_id = #{businessId}
                    </if>
                    <if test="businessName != null and businessName != ''">
                        and s.business_name = #{businessName}
                    </if>
        </where>
    </sql>

    <sql id="order">
        <if test="(ascs != null ascs != '') or (descs != null descs != '')">
            order by
            <if test="ascs != null and ascs != ''">
                ${ascs} asc
            </if>
            <if test="descs != null and descs != ''">
                ${descs} desc
            </if>
        </if>
    </sql>

    <select id="get" parameterType="java.lang.Long" resultType="java.util.HashMap">
        select
        <include refid="fields"/>
        from
        <include refid="table"/>
        where rd.id = #{id}
    </select>

    <select id="list" resultType="java.util.HashMap">
        select
        <include refid="fields"/>
        from
        <include refid="table"/>
        <include refid="where"/>
        <include refid="order"/>
    </select>

    <insert id="insert">
        insert into business_store (
                            name,
                    business_id,
                    business_name
        ) values (
                            #{name},
                    #{businessId},
                    #{businessName}
        )
    </insert>

    <update id="update" parameterType="java.lang.Long">
        update business_store
        <set>
                                    name = #{name},
                        business_id = #{businessId},
                        business_name = #{businessName}
        </set>
        where id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        delete from
        <include refid="table"/>
        where s.id = #{id}
    </delete>

</mapper>